@model IEnumerable<GörevYöneticisiMulakatTemel.ViewModel.JobsVM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("İş Ekle", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobComment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JobDefinition)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.JobType)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.JobComment)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.JobDate)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.JobDefinition)
    </td>
    <td>
        @Html.ActionLink("Düzenle", "Edit", new { id = item.JobId }) |
        @Html.ActionLink("Sil", "Delete", new { id = item.JobId })
    </td>
</tr>
}

</table>
